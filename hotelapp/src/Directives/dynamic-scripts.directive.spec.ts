import { DynamicScriptsDirective } from './dynamic-scripts.directive';

describe('DynamicScriptsDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicScriptsDirective();
    expect(directive).toBeTruthy();
  });
});
